<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Portrait only, like the web manifest: the board and HUD are laid out for it. -->
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Google AdMob app id (@capacitor-community/admob). Value lives in
             res/values/strings.xml; the SDK refuses to start without it. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/admob_app_id" />

        <!-- Play Games Services project id (cloud save via capacitor-cloudsave).
             Must be a string resource: the numeric id overflows an int attribute. -->
        <meta-data
            android:name="com.google.android.gms.games.APP_ID"
            android:value="@string/game_services_project_id" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Play Billing (@capgo/native-purchases) -->
    <uses-permission android:name="com.android.vending.BILLING" />
    <!-- The game's haptics use navigator.vibrate inside the web view. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- AdMob: the Google Mobile Ads SDK declares this itself on Android 13+;
         listed here so the Play Console data-safety answers have no surprises. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
</manifest>
